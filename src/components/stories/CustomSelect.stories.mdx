import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import CustomSelect from "../CustomSelect";
import "./style.sass";
import "./input.sass";
import { FAVORITE_COLORS } from "./SELECT_OPTIONS.ts";

<Meta title="Components/CustomSelect" component={CustomSelect} />

# CustomSelect

## Default

```jsx
<CustomSelect label="Label name" options={FAVORITE_COLORS} name="default-custom-select" />
```

<Canvas>
	<Story
		name="Default"
		args={{
			name: "default-custom-select",
			label: "Label name",
			options: FAVORITE_COLORS
		}}>
		{(args) => <CustomSelect {...args} />}
	</Story>
</Canvas>

## With Value

```jsx
<CustomSelect value="gr7een" label="Label name" options={FAVORITE_COLORS} name="with-value-custom-select" />
```

<Canvas>
	<Story
		name="With Value"
		args={{
			name: "with-value-custom-select",
			label: "Label name",
			options: FAVORITE_COLORS,
			value: "gr7een"
		}}>
		{(args) => <CustomSelect {...args} />}
	</Story>
</Canvas>
